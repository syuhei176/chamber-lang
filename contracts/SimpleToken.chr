 contract SimpleToken {
 
  constructor(){
    this.owners = [msg.sender]
  }

  clause transfer(address to) {
    require(this.owners.length == 1, "Owner isn't only 1")
    require(msg.sender == this.owners, "Owner isn't exactly same as Tx initiator")
    this.owners = [to];
  }
 } 