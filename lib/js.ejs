class <%= contract.name %> {

  <%
    for(var i=0;i < contract.clauses.length;i++) {
      const clause = contract.clauses[i];
  %>
    /**
    * @description <%= contract.name %>.<%= clause.name%>
    */
    static function <%= clause.name %>(tx) {
      /*
        [annotations]
        <% if (clause.annotations && clause.annotations.length > 0) { %>
          <%-
            clause.annotations.map(function(a){
              var strArg = (a.args) ? 
                a.args.map(arg=>{
                  return "(" + arg.plasmaType + ' ' + arg.plasmaValue + ' => ' + arg.ethType + ")"
                }).join(", ")
              : ""
              return a.name + '  ' + strArg
            }).join("\n          ")
          %>
        <% } %>
      */
      
    }
  <% } %>
  
  }
  